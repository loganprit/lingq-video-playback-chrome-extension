{
  "name": "lingq-sentence-playback-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Private Chrome extension for Migaku-style sentence playback controls in LingQ.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/player-bridge.js && node --check src/content.js && node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8'))\""
  }
}
