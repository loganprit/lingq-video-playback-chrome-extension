{
  "name": "lingq-sentence-playback-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Private Chrome extension for Migaku-style sentence playback controls in LingQ.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/content.js && node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8'))\"",
    "prototype:controls": "aside repl \"await attachActiveBrowserTab(); await page.cdp.send('Extensions.loadUnpacked',{path:'$PWD/prototype/control-placement'}); await page.reload(); console.log(page.url())\""
  }
}
